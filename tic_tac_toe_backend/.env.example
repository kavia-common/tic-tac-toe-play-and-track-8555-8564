# Copy this file to .env and set proper values. Do NOT commit real secrets.
PORT=3001
HOST=0.0.0.0
# JWT secret for signing tokens. The orchestrator will inject the actual value at runtime.
JWT_SECRET=change_me_strong_secret
